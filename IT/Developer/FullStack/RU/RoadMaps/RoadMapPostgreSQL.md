## **🗺️ Дорожная карта по PostgreSQL (2025)**
### **🎯 Цели**:
1. Освоить **основы SQL и специфику PostgreSQL** (от запросов до администрирования).
2. Научиться **интегрировать PostgreSQL** с **C# (Entity Framework Core, Dapper)**, **Blazor** и **MAUI**.
3. Изучить **расширенные возможности**:
   - Работа с **JSON/JSONB**, **полнотекстовым поиском**, **геоданными (PostGIS)**.
   - Оптимизация для **IoT** (временные ряды с **TimescaleDB**).
   - Репликация и масштабирование.
4. Применить знания в **реальных проектах** (например, дашборды для IoT, аналитика данных).

---

## **📌 Этап 1: Установка и основы SQL (1 неделя)**
### **Что изучить**:
1. **Установка и настройка**:
   - Установка PostgreSQL на **Windows/Linux** (или Docker).
   - Инструменты: **pgAdmin**, **DBeaver**, **psql** (CLI).
   - Создание первой БД и пользователя:
     ```sql
     CREATE DATABASE my_first_db;
     CREATE USER admin WITH PASSWORD 'secure_password';
     GRANT ALL PRIVILEGES ON DATABASE my_first_db TO admin;
     ```

2. **Основы SQL**:
   - **DDL** (Data Definition Language):
     - Создание таблиц (`CREATE TABLE`), индексов (`CREATE INDEX`).
     - Изменение структуры (`ALTER TABLE`).
   - **DML** (Data Manipulation Language):
     - `SELECT`, `INSERT`, `UPDATE`, `DELETE`.
     - Фильтрация (`WHERE`, `LIKE`, `IN`), сортировка (`ORDER BY`), агрегация (`GROUP BY`, `HAVING`).
   - **Транзакции**:
     - `BEGIN`, `COMMIT`, `ROLLBACK`.
     - Уровни изоляции (`READ COMMITTED`, `REPEATABLE READ`).

3. **Особенности PostgreSQL**:
   - Типы данных: `SERIAL`, `UUID`, `JSONB`, `ARRAY`.
   - Последовательности (`SEQUENCE`).
   - Функции и операторы (например, `ILIKE` для регистронезависимого поиска).

### **Практика**:
- **Задача 1**: Создайте БД для **библиотеки** с таблицами:
  - `books` (id, title, author_id, published_date, isbn).
  - `authors` (id, name, birth_date).
  - `users` (id, name, email).
  - `loans` (id, book_id, user_id, loan_date, return_date).
- **Задача 2**: Напишите запросы:
  - Вывести все книги автора "Толстой".
  - Найти пользователей, которые не вернули книги вовремя.
  - Обновить email пользователя.

### **Ресурсы**:
- [Официальная документация PostgreSQL](https://www.postgresql.org/docs/current/)
- [Установка PostgreSQL на Windows](https://www.postgresql.org/download/windows/)
- [SQL Tutorial (W3Schools)](https://www.w3schools.com/sql/)
- Книга: *"PostgreSQL 15 Internals"* (для глубокого понимания).

---

## **📌 Этап 2: Продвинутые запросы и функции (2 недели)**
### **Что изучить**:
1. **Сложные запросы**:
   - Подзапросы (`IN`, `EXISTS`, `WITH` (CTE)).
   - Объединения (`JOIN`: `INNER`, `LEFT`, `RIGHT`, `FULL`).
   - Оконные функции (`OVER`, `PARTITION BY`, `ROW_NUMBER`, `RANK`).

2. **Функции и процедуры**:
   - Создание функций на **PL/pgSQL**:
     ```sql
     CREATE FUNCTION get_user_loans(user_id INT) RETURNS TABLE(book_title TEXT, loan_date DATE) AS $$
     BEGIN
         RETURN QUERY
         SELECT b.title, l.loan_date
         FROM loans l
         JOIN books b ON l.book_id = b.id
         WHERE l.user_id = user_id;
     END;
     $$ LANGUAGE plpgsql;
     ```
   - Триггеры (`BEFORE INSERT`, `AFTER UPDATE`).

3. **Оптимизация запросов**:
   - Чтение планов выполнения (`EXPLAIN ANALYZE`).
   - Индексы: когда создавать, типы (`B-tree`, `Hash`, `GIN`, `GiST`).
   - Партиционирование таблиц (`BY RANGE`, `BY LIST`).

### **Практика**:
- **Задача 3**: Напишите функцию для **расчета штрафов** за просроченные книги.
- **Задача 4**: Создайте **триггер**, который обновляет поле `last_updated` при изменении записи.
- **Задача 5**: Оптимизируйте запрос для вывода **топ-10 самых популярных книг** (с количеством выдач).

### **Ресурсы**:
- [Оконные функции в PostgreSQL](https://www.postgresql.org/docs/current/tutorial-window.html)
- [PL/pgSQL](https://www.postgresql.org/docs/current/plpgsql.html)
- [EXPLAIN ANALYZE](https://www.postgresql.org/docs/current/using-explain.html)

---

## **📌 Этап 3: Интеграция с C# (.NET) (2 недели)**
### **Что изучить**:
1. **Entity Framework Core**:
   - Настройка подключения:
     ```csharp
     services.AddDbContext<AppDbContext>(options =>
         options.UseNpgsql("Host=localhost;Database=my_db;Username=admin;Password=secure_password"));
     ```
   - Миграции (`Add-Migration`, `Update-Database`).
   - Отношения между сущностями (`One-to-Many`, `Many-to-Many`).
   - Запрос данных (`LINQ`, `FromSqlRaw`).

2. **Dapper**:
   - Легковесная альтернатива EF Core для сложных запросов.
   - Пример:
     ```csharp
     using var connection = new NpgsqlConnection(connectionString);
     var users = connection.Query<User>("SELECT * FROM users WHERE active = @Active", new { Active = true });
     ```

3. **Асинхронные операции**:
   - `ToListAsync`, `FirstOrDefaultAsync`.
   - Транзакции:
     ```csharp
     using var transaction = await dbContext.Database.BeginTransactionAsync();
     try {
         await dbContext.Books.AddAsync(book);
         await dbContext.SaveChangesAsync();
         await transaction.CommitAsync();
     } catch {
         await transaction.RollbackAsync();
     }
     ```

4. **Обработка JSON**:
   - Работа с `JSONB` в PostgreSQL:
     ```sql
     CREATE TABLE user_profiles (
         id SERIAL PRIMARY KEY,
         data JSONB NOT NULL
     );
     ```
   - Запросы в C#:
     ```csharp
     var profiles = dbContext.UserProfiles.Where(p => p.Data["premium"].Value<bool>());
     ```

### **Практика**:
- **Задача 6**: Создайте **ASP.NET Core API** для библиотеки с:
  - CRUD-операциями для книг и авторов.
  - Эндпоинтом для поиска книг по автору (`/api/books?author=Толстой`).
  - Миграциями для обновления схемы БД.
- **Задача 7**: Напишите **Blazor-приложение** для управления библиотекой с:
  - Отображением списка книг (с пагинацией).
  - Формой добавления новой книги.

### **Ресурсы**:
- [EF Core + PostgreSQL](https://learn.microsoft.com/ru-ru/ef/core/providers/npgsql/)
- [Dapper](https://github.com/DapperLib/Dapper)
- [Работа с JSONB в EF Core](https://www.npgsql.org/efcore/mapping/json.html)

---

## **📌 Этап 4: Расширенные возможности PostgreSQL (2 недели)**
### **Что изучить**:
1. **Полнотекстовый поиск**:
   - Типы `tsvector`, `tsquery`.
   - Пример:
     ```sql
     SELECT title FROM books
     WHERE to_tsvector('russian', title) @@ to_tsquery('russian', 'война & мир');
     ```
   - Интеграция с C# (использование `NpgsqlTsVector`).

2. **PostGIS (геоданные)**:
   - Установка расширения:
     ```sql
     CREATE EXTENSION postgis;
     ```
   - Типы `POINT`, `POLYGON`.
   - Запросы:
     ```sql
     SELECT name FROM cities
     WHERE ST_DWithin(geolocation, ST_MakePoint(37.6176, 55.7558), 10000);
     ```
   - Визуализация на карте в Blazor (например, с `Leaflet`).

3. **JSON/JSONB**:
   - Запросы с `->`, `->>`, `@>`.
   - Индексы для JSONB (`GIN`).
   - Пример:
     ```sql
     SELECT * FROM user_profiles
     WHERE data @> '{"premium": true, "subscription": {"type": "yearly"}}';
     ```

4. **Временные ряды (TimescaleDB)**:
   - Установка расширения:
     ```sql
     CREATE EXTENSION IF NOT EXISTS timescaledb;
     ```
   - Создание гипертаблицы:
     ```sql
     SELECT create_hypertable('sensor_data', 'time');
     ```
   - Запросы для агрегации данных (например, средняя температура за час).

### **Практика**:
- **Задача 8**: Добавьте **полнотекстовый поиск** в приложение библиотеки:
  - Поиск книг по ключевым словам (с ранжированием релевантности).
- **Задача 9**: Создайте БД для **сервиса такси** с:
  - Таблицей `rides` (маршруты с координатами начала и конца).
  - Запросами для поиска водителей в радиусе 5 км от пользователя.
- **Задача 10**: Реализуйте **хранение данных с датчиков** (IoT) с:
  - Гипертаблицей для температуры/влажности.
  - Запросами для построения графиков (например, средняя температура за день).

### **Ресурсы**:
- [PostGIS](https://postgis.net/)
- [TimescaleDB](https://docs.timescale.com/)
- [Полнотекстовый поиск](https://www.postgresql.org/docs/current/textsearch.html)

---

## **📌 Этап 5: Оптимизация и масштабирование (1 неделя)**
### **Что изучить**:
1. **Индексы и производительность**:
   - Когда создавать индексы (и когда не надо).
   - Частичные индексы (`WHERE` в определении индекса).
   - Мультиколонные индексы.

2. **Репликация и кластеризация**:
   - Настройка **master-slave репликации**.
   - Использование **pgpool** для балансировки нагрузки.
   - Горизонтальное масштабирование (шардинг).

3. **Мониторинг и логирование**:
   - Логи медленных запросов (`log_min_duration_statement`).
   - Инструменты: **pgBadger**, **pg_stat_statements**.
   - Настройка `postgresql.conf` для оптимизации производительности.

4. **Бэкапы и восстановление**:
   - `pg_dump`, `pg_restore`.
   - Инкрементальные бэкапы с **WAL-архивацией**.

### **Практика**:
- **Задача 11**: Оптимизируйте запрос из **Задачи 5** (топ-10 книг) с помощью индексов.
- **Задача 12**: Настройте **репликацию** для БД библиотеки (master-slave).
- **Задача 13**: Создайте **скрипт для бэкапа** и восстановления БД.

### **Ресурсы**:
- [Оптимизация PostgreSQL](https://wiki.postgresql.org/wiki/Performance_Optimization)
- [Репликация](https://www.postgresql.org/docs/current/warm-standby.html)
- [pgBadger](https://pgbadger.darold.net/)

---

## **📌 Этап 6: PostgreSQL для IoT и аналитики (1 неделя)**
### **Что изучить**:
1. **Хранение данных с датчиков**:
   - Оптимальные схемы для временных рядов.
   - Пример таблицы для IoT:
     ```sql
     CREATE TABLE sensor_readings (
         time TIMESTAMPTZ NOT NULL,
         sensor_id INTEGER NOT NULL,
         temperature DOUBLE PRECISION,
         humidity DOUBLE PRECISION
     );
     SELECT create_hypertable('sensor_readings', 'time');
     ```

2. **Агрегация и аналитика**:
   - Оконные функции для анализа трендов.
   - Пример: средняя температура по часам:
     ```sql
     SELECT
         time_bucket('1 hour', time) AS hour,
         sensor_id,
         AVG(temperature) AS avg_temp
     FROM sensor_readings
     GROUP BY hour, sensor_id
     ORDER BY hour;
     ```

3. **Интеграция с Kafka/ELK**:
   - Потоковая обработка данных (например, с помощью **Debezium** для CDC).
   - Экспорт данных в **Elasticsearch** для визуализации в Kibana.

4. **Визуализация в Blazor/MAUI**:
   - Подключение к PostgreSQL из Blazor (через API или прямое соединение).
   - Построение графиков с **LiveCharts** или **Chart.js**.

### **Практика**:
- **Задача 14**: Создайте **систему мониторинга IoT** с:
  - Записью данных от 10 датчиков (симулируйте данные скриптом на Python).
  - Запросами для вывода **максимальной/минимальной температуры за день**.
  - Дашбордом в Blazor с графиками (`LiveCharts`).
- **Задача 15**: Настройте **экспорт данных** в Elasticsearch для визуализации в Kibana.

### **Ресурсы**:
- [TimescaleDB для IoT](https://www.timescale.com/use-cases/iot)
- [Debezium для CDC](https://debezium.io/)
- [LiveCharts для Blazor](https://github.com/beto-rodriguez/LiveCharts2)

---

## **📌 Этап 7: Реальные проекты и портфолио (постоянно)**
### **Что сделать**:
1. **Проекты для портфолио**:
   - **Система бронирования отелей**:
     - БД с комнатами, бронями, пользователями.
     - Полнотекстовый поиск по описанию отелей.
     - Интеграция с Blazor (админка) и MAUI (мобильное приложение).
   - **Платформа для анализа IoT-данных**:
     - Хранение данных с датчиков в TimescaleDB.
     - Дашборд в Blazor с графиками и уведомлениями.
   - **Социальная сеть для книголюбов**:
     - Поиск по книгам/авторам, рекомендации (на основе JSONB с метками).
     - Геолокация пользователей (PostGIS).

2. **Опубликуйте код**:
   - Загрузите проекты на **GitHub** с подробным README.
   - Напишите посты на **Habr/Dev.to** о вашем опыте.

3. **Участие в сообществе**:
   - Отвечайте на вопросы на **Stack Overflow** (тег `postgresql`).
   - Участвуйте в **PostgreSQL-митапах** (например, [PGConf.Russia](https://pgconf.ru/)).

---
## **📅 Примерный график (2–3 месяца)**
| Этап                     | Длительность | Результат                                  |
|--------------------------|--------------|--------------------------------------------|
| Установка и основы SQL   | 1 неделя      | БД для библиотеки, простые запросы        |
| Продвинутые запросы      | 2 недели      | Функции, триггеры, оконные функции         |
| Интеграция с C#          | 2 недели      | ASP.NET API + Blazor для библиотеки       |
| Расширенные возможности  | 2 недели      | Полнотекстовый поиск, PostGIS, JSONB       |
| Оптимизация              | 1 неделя      | Индексы, репликация, бэкапы                |
| IoT и аналитика          | 1 неделя      | Дашборд для IoT-данных                     |
| Проекты для портфолио    | Постоянно    | 2–3 завершённых проекта на GitHub          |

---

## **💡 Советы от Senior Developer**
1. **Используйте правильные типы данных**:
   - Для денег: `DECIMAL(10, 2)`, а не `FLOAT`.
   - Для идентификаторов: `UUID` вместо `SERIAL`, если нужна распределённость.
   - Для JSON: `JSONB` (бинарный формат быстрее, чем `JSON`).

2. **Оптимизируйте запросы**:
   - Всегда проверяйте план выполнения (`EXPLAIN ANALYZE`).
   - Избегайте `SELECT *` — указывайте только нужные колонки.
   - Используйте `JOIN` вместо подзапросов, где это возможно.

3. **Бэкапы — это свято**:
   - Настройте **автоматические бэкапы** (например, с помощью `cron` + `pg_dump`).
   - Тестируйте восстановление!

4. **Для IoT-данных**:
   - Используйте **TimescaleDB** для временных рядов — это сильно упрощает агрегацию.
   - Партиционируйте данные по времени (например, по месяцам).

5. **Интеграция с C#**:
   - Для простых проектов хватит **EF Core**.
   - Для высоконагруженных систем рассмотрите **Dapper** или **SQL-скрипты**.

6. **Безопасность**:
   - Никогда не используйте **конкатенацию строк** для формирования запросов (SQL-инъекции!).
   - Используйте **параметризованные запросы**:
     ```csharp
     var users = connection.Query<User>("SELECT * FROM users WHERE email = @Email", new { Email = userEmail });
     ```
   - Ограничивайте права пользователей БД (принцип наименьших привилегий).

---
## **🚀 Пример реального проекта для портфолио**
**Название**: *"SmartHome Analytics Platform"*
**Стек**:
- **PostgreSQL + TimescaleDB**: хранение данных с датчиков (температура, влажность, движение).
- **PostGIS**: хранение планов помещений (например, расположение датчиков на карте).
- **C# (ASP.NET Core)**: API для записи/чтения данных.
- **Blazor Hybrid (MAUI)**: мобильное приложение для мониторинга.
- **Python**: скрипты для симуляции данных и обучения моделей (например, предсказание отказов оборудования).

**Фичи**:
✅ **Реальное время**: данные обновляются каждую секунду (SignalR).
✅ **Аналитика**: графики температуры/влажности за день/неделю/месяц.
✅ **Уведомления**: оповещения при превышении порогов (например, "Температура > 30°C!").
✅ **Геолокация**: отображение датчиков на плане дома (PostGIS + Leaflet).
✅ **Офлайн-режим**: кэширование данных в SQLite (для MAUI).