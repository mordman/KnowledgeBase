## **🗺️ Дорожная карта по .NET MAUI (2025)**
### **🎯 Цель**:
Создать **кроссплатформенное приложение** (Windows, Android, iOS, macOS) с использованием **C#**, **XAML**, **MVVM**, интеграцией с **Blazor Hybrid** и **бэкендом** (ASP.NET Core + PostgreSQL).

---
## **📌 Этап 1: Основы .NET MAUI (1–2 недели)**
### **Что изучить**:
1. **Архитектура MAUI**:
   - Чем отличается от Xamarin.Forms?
   - Структура проекта: `Platforms/`, `Resources/`, `AppShell.xaml`.
   - Жизненный цикл приложения (`App.xaml.cs`, `MauiProgram.cs`).

2. **XAML + C#**:
   - Базовые элементы разметки: `<VerticalStackLayout>`, `<Label>`, `<Button>`, `<Entry>`.
   - Привязка данных (`Binding`), команды (`Command`).
   - Стили и ресурсы (`StaticResource`, `DynamicResource`).

3. **MVVM (Model-View-ViewModel)**:
   - Паттерн, его преимущества для MAUI.
   - Библиотеки: **CommunityToolkit.MVVM** (ранее `Microsoft.Toolkit.Mvvm`).
   - Пример: привязка кнопки к команде во ViewModel.

### **Практика**:
- **Задача 1**: Создайте приложение "Список дел" (Todo-list) с:
  - Добавлением/удалением задач.
  - Сохранением в `Preferences` (локальное хранилище).
- **Задача 2**: Реализуйте калькулятор с привязкой данных через `INotifyPropertyChanged`.

### **Ресурсы**:
- [Официальный туториал MAUI](https://learn.microsoft.com/ru-ru/dotnet/maui/get-started/first-app?)
- [CommunityToolkit.MVVM](https://learn.microsoft.com/ru-ru/dotnet/communitytoolkit/mvvm/)
- Книга: *"Creating Mobile Apps with .NET MAUI"* (Charles Petzold).

---
## **📌 Этап 2: Продвинутые элементы UI и навигация (2–3 недели)**
### **Что изучить**:
1. **Навигация**:
   - `Shell`-навигация (регистрация маршрутов в `AppShell.xaml`).
   - Передача параметров между страницами (`QueryProperty`, `NavigationParameters`).
   - Модальные окна (`DisplayAlert`, `DisplayActionSheet`).

2. **Коллекции и списки**:
   - `CollectionView` vs `ListView`.
   - Группировка, pull-to-refresh, бесконечная прокрутка.
   - Пример: список новостей с подгрузкой данных.

3. **Анимации и эффекты**:
   - Базовые анимации (`TranslateTo`, `FadeTo`).
   - Кастомные рендереры (если нужно платформенно-специфичное поведение).

4. **Доступ к платформенным фичам**:
   - Камера, геолокация, акселерометр (`Permissions`, `DeviceSensor`).
   - Пример: приложение, которое фотографирует и сохраняет изображение.

### **Практика**:
- **Задача 3**: Создайте приложение "Галерея" с:
  - Загрузкой фото с камеры/галереи.
  - Отображением в `CollectionView` с анимациями.
  - Возможностью делиться фото (используйте `Share` API).
- **Задача 4**: Реализуйте погодное приложение с:
  - Данными из публичного API (например, [OpenWeatherMap](https://openweathermap.org/api)).
  - Кэшированием ответов в `Preferences`.

### **Ресурсы**:
- [Документация по CollectionView](https://learn.microsoft.com/ru-ru/dotnet/maui/user-interface/controls/collectionview/)
- [Работа с разрешениями](https://learn.microsoft.com/ru-ru/dotnet/maui/platform-integration/permissions?view=net-maui-7.0)
- Видео: [MAUI UI Challenges](https://www.youtube.com/c/JamesMontemagno) (канал Джеймса Монтеманьо).

---
## **📌 Этап 3: Работа с данными и бэкендом (2–3 недели)**
### **Что изучить**:
1. **Локальное хранилище**:
   - `Preferences` (простые ключ-значение).
   - `SQLite` (через `SQLite-net` или `EF Core`).
   - Пример: офлайн-режим для Todo-list.

2. **Сетевые запросы**:
   - `HttpClient` для работы с REST API.
   - Библиотеки: `Refit` (упрощает работу с API).
   - Обработка ошибок и ретраи (`Polly`).

3. **Интеграция с ASP.NET Core**:
   - Создание бэкенда на **ASP.NET Core** (например, для Todo-list).
   - Аутентификация: **JWT** или **Azure AD**.
   - Пример: синхронизация данных между MAUI и бэкендом.

4. **SignalR**:
   - Реальное время (например, чат или дашборд IoT).

### **Практика**:
- **Задача 5**: Разработайте **кроссплатформенный клиент для блога** с:
  - Получением постов из ASP.NET Core API.
  - Возможностью добавлять комментарии (с аутентификацией).
  - Офлайн-режимом (данные сохраняются в SQLite и синхронизируются при подключении).
- **Задача 6**: Создайте **простой мессенджер** с:
  - Отправкой сообщений через SignalR.
  - Уведомлениями (используйте `Toast` или платформенные нотификации).

### **Ресурсы**:
- [Refit для MAUI](https://github.com/reactiveui/refit)
- [Polly для ретраев](https://github.com/App-vNext/Polly)
- [SignalR в MAUI](https://learn.microsoft.com/ru-ru/aspnet/core/signalr/clients/?view=aspnetcore-7.0)

---
## **📌 Этап 4: Blazor Hybrid в MAUI (2 недели)**
### **Что изучить**:
1. **Интеграция Blazor**:
   - Добавление `BlazorWebView` в MAUI-проект.
   - Обмен данными между MAUI и Blazor (через `JSInterop`).
   - Пример: встраивание дашборда на Blazor в MAUI-приложение.

2. **Общие библиотеки**:
   - Разделение логики между MAUI и Blazor (например, сервисы для работы с API).

3. **Оптимизация производительности**:
   - Ленивая загрузка компонентов Blazor.
   - Кэширование данных.

### **Практика**:
- **Задача 7**: Создайте **гибридное приложение "Фитнес-трекер"** с:
  - MAUI для основного UI (навигация, камера для сканирования QR-кодов).
  - Blazor для отображения графиков тренировок (используйте `Chart.js`).
  - Данные хранятся в ASP.NET Core + PostgreSQL.

### **Ресурсы**:
- [Документация по Blazor Hybrid](https://learn.microsoft.com/ru-ru/aspnet/core/blazor/hybrid/?view=aspnetcore-7.0)
- [Пример проекта](https://github.com/dotnet/maui-samples/tree/main/Blazor/BlazorHybrid)

---
## **📌 Этап 5: IoT + MAUI (2 недели)**
### **Что изучить**:
1. **Подключение к устройствам**:
   - Работа с **MQTT** (библиотека `MQTTnet`).
   - Пример: получение данных с ESP32 (температура, влажность).

2. **Обработка данных в реальном времени**:
   - Отображение данных на графиках (`Microcharts` или `LiveCharts`).
   - Уведомления при превышении порогов (например, "Температура слишком высока!").

3. **Интеграция с AI**:
   - Отправка данных на бэкенд для анализа (Python + FastAPI).
   - Пример: распознавание образов на ESP32 → отправка результата в MAUI.

### **Практика**:
- **Задача 8**: Разработайте **мониторинг умного дома** с:
  - Данными с ESP32 (MQTT).
  - Визуализацией в MAUI (графики, уведомления).
  - Возможностью управлять устройствами (включать/выключать реле).

### **Ресурсы**:
- [MQTT в MAUI](https://github.com/dotnet/MQTTnet)
- [LiveCharts2 для графиков](https://github.com/beto-rodriguez/LiveCharts2)

---
## **📌 Этап 6: Оптимизация и публикация (1 неделя)**
### **Что изучить**:
1. **Производительность**:
   - Профилирование памяти (`Visual Studio Diagnostic Tools`).
   - AOT-компиляция для уменьшения размера приложения.

2. **Платформенные особенности**:
   - Настройка для **Android** (`AndroidManifest.xml`, разрешения).
   - Настройка для **iOS** (`Info.plist`, provisioning profiles).

3. **Публикация**:
   - Сборка для **Google Play** и **App Store**.
   - **Windows Package** (MSIX).

### **Практика**:
- **Задача 9**: Оптимизируйте и опубликуйте одно из предыдущих приложений (например, "Фитнес-трекер") в **Google Play** (или тестовом треке).

### **Ресурсы**:
- [Публикация MAUI в stores](https://learn.microsoft.com/ru-ru/dotnet/maui/deploy/)
- [Оптимизация производительности](https://learn.microsoft.com/ru-ru/dotnet/maui/performance/)

---
## **📌 Этап 7: Портфолио и дальнейшее развитие**
### **Что сделать**:
1. **Соберите портфолио**:
   - Опубликуйте исходный код проектов на **GitHub** (с хорошим README).
   - Запишите демо-видео (можно на YouTube).

2. **Изучите дополнительные темы**:
   - **SkiaSharp** для кастомного рендеринга.
   - **MAUI + Unity** (если интересуют игры).
   - **AI в MAUI**: интеграция с **ONNX Runtime** для инференса на устройстве.

3. **Сообщество**:
   - Участвуйте в **MAUI Discord** ([ссылка](https://discord.gg/dotnet)).
   - Следите за обновлениями (.NET 9 выйдет в ноябре 2025!).

---
## **📅 Примерный график (2.5–3 месяца)**
| Этап               | Длительность | Результат                                  |
|--------------------|--------------|--------------------------------------------|
| Основы MAUI        | 1–2 недели    | Todo-list, калькулятор                     |
| Продвинутый UI     | 2–3 недели    | Галерея, погодное приложение               |
| Данные + бэкенд    | 2–3 недели    | Блог, мессенджер                           |
| Blazor Hybrid      | 2 недели      | Фитнес-трекер                              |
| IoT + MAUI         | 2 недели      | Умный дом                                 |
| Оптимизация        | 1 неделя      | Публикация в store                         |
| Портфолио          | Постоянно    | GitHub, демо-видео                         |

---
## **💡 Советы от Senior Developer**
1. **Не бойтесь платформенного кода**:
   - Иногда нужно писать нативный код для Android/iOS (например, для кастомных вьюх). Используйте `#if ANDROID`/`#if IOS`.

2. **Тестируйте на реальных устройствах**:
   - Эмуляторы не всегда показывают реальные баги (например, с производительностью).

3. **Используйте CommunityToolkit**:
   - Ускоряет разработку (например, `Behavior`, `Converter`, `Guard`).

4. **Монолиты vs Микросервисы**:
   - Для мобильных приложений часто достаточно **одного бэкенда** (ASP.NET Core) вместо микросервисов.

5. **IoT + MAUI**:
   - Для передачи данных с ESP32 используйте **MQTT** (легче, чем HTTP для устройств с ограниченными ресурсами).

---
## **🚀 Пример реального проекта для портфолио**
**Название**: *"SmartHome Dashboard"*
**Стек**:
- **MAUI** (основной UI + камера для сканирования QR-кодов устройств).
- **Blazor Hybrid** (дашборд с графиками данных).
- **ASP.NET Core** (бэкенд для хранения данных).
- **PostgreSQL** (хранение логов устройств).
- **ESP32** (отправка данных по MQTT).
- **Python** (обучение модели для распознавания лиц на ESP32).

**Фичи**:
✅ Реальное время (SignalR).
✅ Офлайн-режим (SQLite).
✅ Уведомления при событиях (например, "Дверь открыта!").
✅ Кроссплатформенность (Android/iOS/Windows).