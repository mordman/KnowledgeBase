### **📋 Чек-лист: Анализ задачи на разработку**

#### **1. Понимание задачи**
- [ ] **Цель задачи**:
  - Чётко сформулирована ли цель? (Пример: *"Разработать API для управления заказами с поддержкой Kafka-стриминга"*).
  - Есть ли примеры аналогичных решений или референсы?
- [ ] **Контекст**:
  - Как задача вписывается в общую архитектуру проекта?
  - Кто конечные пользователи? (Внутренний сервис, внешние клиенты, админ-панель?)
- [ ] **Критерии приёмки (Acceptance Criteria)**:
  - Есть ли чёткие условия завершённости? (Пример: *"API должен обрабатывать 500 RPS с задержкой <200мс"*).

---

#### **2. Технические требования**
- [ ] **Стек технологий**:
  - Язык/фреймворк: **C# / ASP.NET Core / Blazor**?
  - База данных: **SQL Server / PostgreSQL / MongoDB**?
  - Брокеры сообщений: **Kafka / RabbitMQ**?
  - Инфраструктура: **Docker / Kubernetes / Azure/AWS**?
- [ ] **Интеграции**:
  - С какими внешними системами нужно взаимодействовать? (Пример: **ELK для логов**, платежные шлюзы, сторонние API).
  - Протоколы: **REST / gRPC / WebSockets**?
- [ ] **Производительность и масштабируемость**:
  - Ожидаемая нагрузка (RPS, объём данных).
  - Нужна ли горизонтальная масштабируемость (например, для высоконагруженных сервисов)?

---

#### **3. Архитектура и дизайн**
- [ ] **Архитектурный стиль**:
  - Монолит / **Микросервисы** / **CQRS** / **Event Sourcing**?
  - Нужна ли **Event-Driven архитектура** (например, для Kafka-интеграции)?
- [ ] **Диаграммы**:
  - Есть ли схема взаимодействия компонентов? (Пример в **Mermaid**):
    ```mermaid
    graph TD
      A[Client] -->|REST| B[ASP.NET Core API]
      B -->|Kafka| C[Stream Processing]
      C -->|SQL| D[PostgreSQL]
    ```
- [ ] **Паттерны проектирования**:
  - Какие паттерны применимы? (**Repository**, **Unit of Work**, **Mediator**, **Strategy**).
  - Нужны ли **Observer** или **Chain of Responsibility** для обработки событий?

---

#### **4. Данные и хранилища**
- [ ] **Модель данных**:
  - Определены ли сущности и их связи? (Пример: `Order → Payment → User`).
  - Нужна ли **историзация данных** (например, для аудита изменений)?
- [ ] **Хранилища**:
  - Реляционная база (**SQL Server/PostgreSQL**) или **NoSQL** (для неструктурированных данных)?
  - Нужны ли специализированные БД: **Redis** (кэш), **Elasticsearch** (поиск/логи)?
- [ ] **Миграции и бэкапы**:
  - Как будут управляться миграции (**EF Core Migrations**, **Flyway**)?
  - Нужен ли план бэкапов?

---

#### **5. Безопасность**
- [ ] **Аутентификация/Авторизация**:
  - Как будут авторизоваться пользователи? (**JWT**, **OAuth2**, **IdentityServer**).
  - Нужна ли **ролевая модель (RBAC)**?
- [ ] **Защита данных**:
  - Шифруются ли чувствительные данные (например, **токены**, **личная информация**)?
  - Соответствие **GDPR** (если работаем с личными данными).
- [ ] **Уязвимости**:
  - Проверены ли зависимости на уязвимости (**OWASP Dependency-Check**, **GitHub Dependabot**)?

---

#### **6. Тестирование**
- [ ] **Виды тестов**:
  - **Юнит-тесты** (xUnit/NUnit) для бизнес-логики.
  - **Интеграционные тесты** (для Kafka, БД, внешних API).
  - **Нагрузочные тесты** (например, **k6** или **JMeter** для API).
- [ ] **CI/CD**:
  - Как будет организован пайплайн (**GitHub Actions**, **Azure DevOps**)?
  - Нужен ли этап статического анализа (**SonarQube**)?

---

#### **7. Развёртывание и мониторинг**
- [ ] **Инфраструктура**:
  - Где будет развёрнуто решение? (**Облако**, **он-премис**, **контейнеры**).
  - Нужен ли **IaC** (**Terraform**, **Ansible**)?
- [ ] **Логирование и мониторинг**:
  - Как будут собираться логи? (**ELK Stack**, **Serilog + Seq**, **Azure Monitor**).
  - Нужен ли мониторинг метрик (**Prometheus + Grafana**)?
- [ ] **Обработка ошибок**:
  - Есть ли план восстановления после сбоев (например, **ретраи для Kafka**, **fallback-механизмы**)?

---

#### **8. Риски и зависимости**
- [ ] **Технические риски**:
  - Есть ли зависимости от внешних сервисов (например, **облачный Kafka-брокер**)?
  - Что будет, если сервис станет недоступен? (**Fallback**, **репликация данных**).
- [ ] **Временные риски**:
  - Реалистичны ли сроки с учётом сложности задачи?
  - Есть ли зависимости от других команд/задач?
- [ ] **Ресурсы**:
  - Хватит ли вычислительных ресурсов (например, для **Kafka-стриминга** или **Blazor-приложений**)?

---

#### **9. Документация**
- [ ] **Для разработчиков**:
  - Нужно ли обновлять **README** (например, как развернуть проект локально)?
  - Есть ли схемы базы данных или **API-документация** (**Swagger/OpenAPI**)?
- [ ] **Для пользователей**:
  - Нужен ли гайд по использованию (например, для **Blazor-приложения** или **админ-панели**)?

---

### **📌 Пример заполнения (фрагмент)**
**Задача**: Разработать сервис уведомлений для платформы заказов с интеграцией через Kafka.

| Пункт               | Детали                                                                                     |
|---------------------|---------------------------------------------------------------------------------------------|
| **Цель**            | Отправлять уведомления пользователям о статусах заказов в реальном времени.                |
| **Стек**            | C#, ASP.NET Core, Kafka, PostgreSQL, Docker.                                               |
| **Архитектура**     | Микросервис: `Notification Service` (Kafka Consumer) → `Email/SMS Gateway`.                  |
| **Безопасность**    | JWT для API, TLS для Kafka, шифрование личных данных пользователей.                       |
| **Тестирование**    | Юнит-тесты для логики уведомлений, интеграционные тесты для Kafka.                          |
| **Мониторинг**      | Логи в ELK, метрики в Prometheus (количество отправленных уведомлений, ошибки).             |

---

### **🔍 Советы по использованию**
1. **Адаптируй под задачу**: Убери нерелевантные пункты (например, если нет Kafka, удали соответствующие вопросы).
2. **Визуализируй**: Используй **Mermaid** для схем архитектуры или потоков данных.
3. **Обсуждай с командой**: Особенно важно для сложных задач — помогает выявить скрытые риски.
4. **Документируй**: Фиксируй ответы в **Confluence**, **README** или **Notion**.

---