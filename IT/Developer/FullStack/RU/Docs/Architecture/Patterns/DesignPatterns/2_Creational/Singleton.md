**Цель:**  
Singleton — это порождающий паттерн проектирования, который **гарантирует существование только одного экземпляра класса** и предоставляет глобальную точку доступа к нему. Он используется, когда требуется централизованное управление общим ресурсом (например, логгер, конфигурация, пул соединений).

> ⚠️ Singleton часто критикуют за введение **глобального состояния**, что усложняет тестирование и нарушает принцип инверсии зависимостей. Используйте его осторожно.

---

**Пример (C#):**

```csharp
// Потокобезопасная реализация с отложенной инициализацией
public sealed class Logger
{
    // Явное указание: один экземпляр
    private static readonly Lazy<Logger> _instance = 
        new Lazy<Logger>(() => new Logger());

    // Запрет внешнего создания
    private Logger() { }

    // Глобальная точка доступа
    public static Logger Instance => _instance.Value;

    public void Log(string message)
    {
        Console.WriteLine($"[LOG] {DateTime.Now:HH:mm:ss} - {message}");
    }
}

// Использование
Logger.Instance.Log("Application started");
```

> 💡 Использование `Lazy<T>` гарантирует:
> - потокобезопасность,
> - ленивую инициализацию,
> - простоту и читаемость.

---

**Антипаттерн:**  
- **Небезопасная многопоточная реализация**:
  ```csharp
  if (_instance == null) _instance = new Logger(); // гонка!
  ```
- **Публичный конструктор или возможность наследования** → нарушение единственности.
- **Использование Singleton вместо DI-контейнера** → жёсткая связность, сложность мокинга в тестах.

---

**Схема (Mermaid):**

```mermaid
classDiagram
    class Logger {
        <<singleton>>
        -{static} _instance: Lazy~Logger~
        -Logger()
        +{static} Instance: Logger
        +Log(message: string)
    }

    Logger ..> Logger : returns same instance via Instance
```

```
    note right of Logger::Instance
        Возвращает единственный
        экземпляр класса.
        Создаётся при первом доступе.
    end note

    note right of Logger::Logger()
        Приватный конструктор
        предотвращает создание
        экземпляров извне.
    end note
```