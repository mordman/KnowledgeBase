## **🗺️ Дорожная карта по Python (2025)**


## Оглавление
- [**🗺️ Дорожная карта по Python (2025)**](#дорожная-карта-по-python-2025)
  - [**🎯 Цели**:](#цели)
- [**📌 Этап 1: Основы Python (2–3 недели)**](#этап-1-основы-python-23-недели)
  - [**Что изучить**:](#что-изучить)
  - [**Практика**:](#практика)
  - [**Ресурсы**:](#ресурсы)
- [**📌 Этап 2: Продвинутые возможности (2 недели)**](#этап-2-продвинутые-возможности-2-недели)
  - [**Что изучить**:](#что-изучить)
  - [**Практика**:](#практика)
  - [**Ресурсы**:](#ресурсы)
- [**📌 Этап 3: Веб-разработка (FastAPI, Django) (3 недели)**](#этап-3-веб-разработка-fastapi-django-3-недели)
  - [**Что изучить**:](#что-изучить)
  - [**Практика**:](#практика)
  - [**Ресурсы**:](#ресурсы)
- [**📌 Этап 4: AI и Machine Learning (3–4 недели)**](#этап-4-ai-и-machine-learning-34-недели)
  - [**Что изучить**:](#что-изучить)
  - [**Практика**:](#практика)
  - [**Ресурсы**:](#ресурсы)
- [**📌 Этап 5: Интеграция Python с C#/.NET (2 недели)**](#этап-5-интеграция-python-с-cnet-2-недели)
  - [**Что изучить**:](#что-изучить)
  - [**Практика**:](#практика)
  - [**Ресурсы**:](#ресурсы)
- [**📌 Этап 6: Python для IoT и встраиваемых систем (2 недели)**](#этап-6-python-для-iot-и-встраиваемых-систем-2-недели)
  - [**Что изучить**:](#что-изучить)
  - [**Практика**:](#практика)
  - [**Ресурсы**:](#ресурсы)
- [**📌 Этап 7: Проекты для портфолио (постоянно)**](#этап-7-проекты-для-портфолио-постоянно)
  - [**Проекты**:](#проекты)
  - [**Как презентовать**:](#как-презентовать)
- [**📅 Примерный график (4–5 месяцев)**](#примерный-график-45-месяцев)
- [**💡 Советы от Senior Developer**](#советы-от-senior-developer)

  - [**🎯 Цели**:](#цели)
  - [**Что изучить**:](#что-изучить)
  - [**Практика**:](#практика)
  - [**Ресурсы**:](#ресурсы)
  - [**Что изучить**:](#что-изучить)
  - [**Практика**:](#практика)
  - [**Ресурсы**:](#ресурсы)
  - [**Что изучить**:](#что-изучить)
  - [**Практика**:](#практика)
  - [**Ресурсы**:](#ресурсы)
  - [**Что изучить**:](#что-изучить)
  - [**Практика**:](#практика)
  - [**Ресурсы**:](#ресурсы)
  - [**Что изучить**:](#что-изучить)
  - [**Практика**:](#практика)
  - [**Ресурсы**:](#ресурсы)
  - [**Что изучить**:](#что-изучить)
  - [**Практика**:](#практика)
  - [**Ресурсы**:](#ресурсы)
  - [**Проекты**:](#проекты)
  - [**Как презентовать**:](#как-презентовать)
### **🎯 Цели**:
1. Освоить **основы Python** и его экосистему (от синтаксиса до асинхронного программирования).
2. Изучить **веб-разработку** (FastAPI, Django) для бэкенда под **Blazor/MAUI**.
3. Погрузиться в **AI/ML** (TensorFlow, PyTorch) с акцентом на **внедрение в IoT** (ESP32, Raspberry Pi).
4. Научиться **интегрировать Python с C#** (gRPC, REST, субпроцессы).
5. Применить знания в **реальных проектах** (например, аналитика IoT-данных из PostgreSQL).

---

## **📌 Этап 1: Основы Python (2–3 недели)**
### **Что изучить**:
1. **Синтаксис и базовые конструкции**:
   - Переменные, циклы (`for`, `while`), условия (`if/elif/else`).
   - Функции (`def`, `lambda`), работа с файлами (`open`, `with`).
   - Структуры данных: списки, кортежи, словари, множества.
   - Обработка исключений (`try/except`).

2. **ООП в Python**:
   - Классы, наследование, инкапсуляция, полиморфизм.
   - Магические методы (`__init__`, `__str__`).
   - Пример:
     ```python
     class Sensor:
         def __init__(self, sensor_id: str, location: str):
             self.sensor_id = sensor_id
             self.location = location
             self.readings = []

         def add_reading(self, value: float):
             self.readings.append(value)

         def get_average(self) -> float:
             return sum(self.readings) / len(self.readings)
     ```

3. **Модули и пакеты**:
   - Импорт (`import`, `from ... import`).
   - Создание собственных модулей.
   - Виртуальные окружения (`venv`, `pip`).

4. **Работа с данными**:
   - Библиотеки: `csv`, `json`, `pandas` (базовые операции).
   - Пример: чтение данных с датчиков из CSV:
     ```python
     import pandas as pd
     data = pd.read_csv("sensor_data.csv")
     print(data.describe())
     ```

### **Практика**:
- **Задача 1**: Напишите **скрипт для анализа логов** (например, подсчёт ошибок в файле).
- **Задача 2**: Создайте **класс для управления датчиками IoT** (методы: добавление данных, расчёт среднего значения).
- **Задача 3**: Реализуйте **простой парсер JSON** (например, для конфигурационного файла IoT-устройства).

### **Ресурсы**:
- [Официальный туториал Python](https://docs.python.org/3/tutorial/)
- [Real Python](https://realpython.com/)
- Книга: *"Fluent Python"* (Луччиано Рамальо).

---

## **📌 Этап 2: Продвинутые возможности (2 недели)**
### **Что изучить**:
1. **Функциональное программирование**:
   - `map`, `filter`, `reduce`, генераторы (`yield`).
   - Декораторы (`@decorator`).
   - Пример:
     ```python
     def log_execution(func):
         def wrapper(*args, **kwargs):
             print(f"Вызов функции {func.__name__}")
             return func(*args, **kwargs)
         return wrapper

     @log_execution
     def calculate_average(readings):
         return sum(readings) / len(readings)
     ```

2. **Асинхронное программирование**:
   - `async/await`, `asyncio`.
   - Пример: асинхронное чтение данных с датчиков:
     ```python
     import asyncio

     async def read_sensor(sensor_id: str):
         print(f"Чтение данных с датчика {sensor_id}...")
         await asyncio.sleep(1)  # Симуляция задержки
         return {"sensor_id": sensor_id, "value": 25.5}

     async def main():
         tasks = [read_sensor(f"sensor_{i}") for i in range(3)]
         results = await asyncio.gather(*tasks)
         print(results)
     ```

3. **Работа с сетью**:
   - Библиотеки: `requests`, `aiohttp` (для HTTP-запросов).
   - Пример: запрос к API погоды:
     ```python
     import requests
     response = requests.get("https://api.openweathermap.org/data/2.5/weather?q=London")
     print(response.json())
     ```

4. **Многопоточность и многопроцессность**:
   - `threading`, `multiprocessing`.
   - Пример: параллельное чтение с нескольких датчиков:
     ```python
     from threading import Thread

     def read_sensor_thread(sensor_id):
         print(f"Датчик {sensor_id}: 25°C")

     threads = [Thread(target=read_sensor_thread, args=(i,)) for i in range(5)]
     for t in threads:
         t.start()
     for t in threads:
         t.join()
     ```

### **Практика**:
- **Задача 4**: Напишите **асинхронный скрипт** для опроса 10 виртуальных датчиков (используйте `asyncio`).
- **Задача 5**: Создайте **декоратор для логирования** времени выполнения функции.
- **Задача 6**: Реализуйте **многопоточный скрипт** для скачивания файлов по URL.

### **Ресурсы**:
- [Асинхронное программирование в Python](https://realpython.com/async-io-python/)
- [Threading vs Multiprocessing](https://realpython.com/python-gil/)

---

## **📌 Этап 3: Веб-разработка (FastAPI, Django) (3 недели)**
### **Что изучить**:
1. **FastAPI**:
   - Создание API-эндпоинтов.
   - Валидация данных с помощью **Pydantic**.
   - Пример:
     ```python
     from fastapi import FastAPI
     from pydantic import BaseModel

     app = FastAPI()

     class SensorData(BaseModel):
         sensor_id: str
         value: float

     @app.post("/sensor")
     async def add_sensor_data(data: SensorData):
         print(f"Получены данные с датчика {data.sensor_id}: {data.value}")
         return {"status": "success"}
     ```
   - Интеграция с **Blazor/MAUI** (например, для отправки данных с IoT-устройств).

2. **Django**:
   - Модели, представления, шаблоны (MVT).
   - Админ-панель, формы, аутентификация.
   - Пример модели для датчиков:
     ```python
     from django.db import models

     class Sensor(models.Model):
         sensor_id = models.CharField(max_length=50, unique=True)
         location = models.CharField(max_length=100)
         created_at = models.DateTimeField(auto_now_add=True)

         def __str__(self):
             return self.sensor_id
     ```

3. **Базы данных**:
   - **SQLite** (для прототипов), **PostgreSQL** (для продакшена).
   - ORM: **SQLAlchemy** (для FastAPI), **Django ORM**.
   - Пример подключения к PostgreSQL в FastAPI:
     ```python
     from sqlalchemy import create_engine
     from sqlalchemy.orm import sessionmaker

     DATABASE_URL = "postgresql://user:password@localhost/dbname"
     engine = create_engine(DATABASE_URL)
     SessionLocal = sessionmaker(autocommit=False, bind=engine)
     ```

4. **Аутентификация**:
   - JWT в FastAPI (`python-jose`, `passlib`).
   - Аутентификация в Django (`django.contrib.auth`).

### **Практика**:
- **Задача 7**: Создайте **API для IoT-устройств** на FastAPI с:
  - Эндпоинтами для добавления/получения данных датчиков.
  - Валидацией данных через Pydantic.
  - Интеграцией с PostgreSQL (хранение данных).
- **Задача 8**: Разработайте **веб-приложение для мониторинга погоды** на Django с:
  - Моделями для городов и погодных данных.
  - Админ-панелью для управления данными.
  - API для получения данных (для использования в Blazor).

### **Ресурсы**:
- [Документация FastAPI](https://fastapi.tiangolo.com/)
- [Документация Django](https://docs.djangoproject.com/)
- [SQLAlchemy](https://www.sqlalchemy.org/)

---

## **📌 Этап 4: AI и Machine Learning (3–4 недели)**
### **Что изучить**:
1. **Основы ML**:
   - Библиотеки: `scikit-learn`, `pandas`, `numpy`.
   - Пример: обучение модели линейной регрессии:
     ```python
     from sklearn.linear_model import LinearRegression
     import numpy as np

     X = np.array([[1], [2], [3], [4]])
     y = np.array([2, 4, 6, 8])
     model = LinearRegression()
     model.fit(X, y)
     print(model.predict([[5]]))  # Выведет 10.0
     ```

2. **Нейронные сети**:
   - **TensorFlow/Keras** для глубокого обучения.
   - Пример: классификация изображений:
     ```python
     from tensorflow.keras import layers, models

     model = models.Sequential([
         layers.Conv2D(32, (3, 3), activation='relu', input_shape=(28, 28, 1)),
         layers.MaxPooling2D((2, 2)),
         layers.Flatten(),
         layers.Dense(10, activation='softmax')
     ])
     model.compile(optimizer='adam', loss='categorical_crossentropy', metrics=['accuracy'])
     ```

3. **Компьютерное зрение**:
   - **OpenCV** для обработки изображений.
   - Пример: детекция лиц на веб-камере:
     ```python
     import cv2

     face_cascade = cv2.CascadeClassifier(cv2.data.haarcascades + 'haarcascade_frontalface_default.xml')
     cap = cv2.VideoCapture(0)

     while True:
         ret, frame = cap.read()
         gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
         faces = face_cascade.detectMultiScale(gray, 1.3, 5)
         for (x, y, w, h) in faces:
             cv2.rectangle(frame, (x, y), (x+w, y+h), (255, 0, 0), 2)
         cv2.imshow('Face Detection', frame)
         if cv2.waitKey(1) & 0xFF == ord('q'):
             break
     ```

4. **Обработка естественного языка (NLP)**:
   - Библиотеки: `nltk`, `spaCy`, `transformers` (Hugging Face).
   - Пример: анализ тональности текста:
     ```python
     from textblob import TextBlob

     text = "Python is amazing!"
     blob = TextBlob(text)
     print(blob.sentiment.polarity)  # Выведет положительное значение
     ```

5. **Внедрение ML в IoT**:
   - **TensorFlow Lite** для микроконтроллеров (ESP32, Raspberry Pi).
   - Пример: конвертация модели для ESP32:
     ```python
     converter = tf.lite.TFLiteConverter.from_keras_model(model)
     tflite_model = converter.convert()
     with open('model.tflite', 'wb') as f:
         f.write(tflite_model)
     ```

### **Практика**:
- **Задача 9**: Обучите модель **линейной регрессии** для предсказания температуры на основе исторических данных (используйте данные из PostgreSQL).
- **Задача 10**: Создайте **скрипт для распознавания объектов** на изображениях (используйте претренированную модель из `tf.keras.applications`).
- **Задача 11**: Реализуйте **детекцию лиц** на веб-камере с сохранением результатов в PostgreSQL.
- **Задача 12**: Конвертируйте обученную модель в **TensorFlow Lite** для развёртывания на ESP32.

### **Ресурсы**:
- [Scikit-learn](https://scikit-learn.org/)
- [TensorFlow Tutorials](https://www.tensorflow.org/tutorials)
- [OpenCV-Python](https://docs.opencv.org/4.x/d6/d00/tutorial_py_root.html)
- [TensorFlow Lite для микроконтроллеров](https://www.tensorflow.org/lite/microcontrollers)

---

## **📌 Этап 5: Интеграция Python с C#/.NET (2 недели)**
### **Что изучить**:
1. **Вызов Python из C#**:
   - Использование **Python.NET** или запуск скриптов через процесс:
     ```csharp
     using (Py.GIL()) // Python.NET
     {
         dynamic np = Py.Import("numpy");
         dynamic arr = np.array(new[] { 1, 2, 3 });
         Console.WriteLine(arr.mean());
     }
     ```
   - Пример с `Process`:
     ```csharp
     var process = new Process
     {
         StartInfo = new ProcessStartInfo
         {
             FileName = "python",
             Arguments = "script.py",
             RedirectStandardOutput = true,
             UseShellExecute = false
         }
     };
     process.Start();
     string output = process.StandardOutput.ReadToEnd();
     process.WaitForExit();
     ```

2. **gRPC для взаимодействия**:
   - Создание **gRPC-сервиса** на Python и клиента на C#.
   - Пример `.proto`-файла:
     ```proto
     syntax = "proto3";
     service SensorService {
         rpc AddReading (SensorReading) returns (Response);
     }
     message SensorReading {
         string sensor_id = 1;
         float value = 2;
     }
     message Response {
         bool success = 1;
     }
     ```
   - Генерация кода для Python и C#:
     ```bash
     python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. sensor.proto
     ```

3. **REST API (FastAPI + C#)**:
   - Создание API на FastAPI для использования в **Blazor/MAUI**.
   - Пример эндпоинта для предсказаний ML-модели:
     ```python
     @app.post("/predict")
     async def predict(data: List[float]):
         model = load_model("model.h5")
         prediction = model.predict([data])
         return {"prediction": prediction.tolist()}
     ```
   - Вызов из C#:
     ```csharp
     var client = new HttpClient();
     var response = await client.PostAsJsonAsync("http://localhost:8000/predict", new[] {1.0f, 2.0f});
     var result = await response.Content.ReadFromJsonAsync<PredictionResponse>();
     ```

4. **Обмен данными через файлы/БД**:
   - Совместное использование **PostgreSQL** (Python записывает, C# читает).
   - Пример: Python скрипт пишет данные в БД, C# (MAUI/Blazor) их визуализирует.

### **Практика**:
- **Задача 13**: Напишите **Python-скрипт для анализа данных** из PostgreSQL и вызовите его из **C# (MAUI)**.
- **Задача 14**: Создайте **gRPC-сервис** на Python для обработки данных с датчиков и клиент на **C# (Blazor)**.
- **Задача 15**: Разработайте **FastAPI-сервис** для предсказаний ML-модели и интегрируйте его с **Blazor-приложением**.

### **Ресурсы**:
- [Python.NET](https://github.com/pythonnet/pythonnet)
- [gRPC для Python](https://grpc.io/docs/languages/python/)
- [Вызов Python из C#](https://stackoverflow.com/questions/14857705/call-python-function-from-c-sharp)

---

## **📌 Этап 6: Python для IoT и встраиваемых систем (2 недели)**
### **Что изучить**:
1. **MicroPython для ESP32/Raspberry Pi**:
   - Установка **MicroPython** на ESP32.
   - Пример: чтение данных с датчика температуры:
     ```python
     from machine import Pin, ADC
     import time

     adc = ADC(Pin(34))  # GPIO34 на ESP32
     while True:
         value = adc.read()
         temperature = (value / 4095) * 100  # Пример преобразования
         print(f"Temperature: {temperature:.2f}°C")
         time.sleep(1)
     ```

2. **Обмен данными по протоколам**:
   - **MQTT** (библиотека `umqtt` для MicroPython).
   - Пример: отправка данных на брокер:
     ```python
     from umqtt.simple import MQTTClient
     import time

     client = MQTTClient("esp32", "broker.emqx.io")
     client.connect()
     while True:
         client.publish("esp32/temperature", str(25.5))
         time.sleep(60)
     ```
   - **HTTP** (запросы к FastAPI-серверу).

3. **Работа с GPIO**:
   - Управление реле, светодиодами, сервоприводами.
   - Пример: мигание светодиодом:
     ```python
     from machine import Pin
     import time

     led = Pin(2, Pin.OUT)  # GPIO2 на ESP32
     while True:
         led.on()
         time.sleep(1)
         led.off()
         time.sleep(1)
     ```

4. **Интеграция с облаком**:
   - Отправка данных в **PostgreSQL** (через FastAPI) или **InfluxDB**.
   - Пример: отправка данных на FastAPI-сервер:
     ```python
     import urequests

     data = {"sensor_id": "esp32_1", "value": 25.5}
     response = urequests.post("http://your-server/api/sensor", json=data)
     print(response.text)
     ```

### **Практика**:
- **Задача 16**: Напишите **скрипт для ESP32**, который отправляет данные температуры по **MQTT** каждую минуту.
- **Задача 17**: Создайте **систему управления светодиодом** через **HTTP-API** (FastAPI + ESP32).
- **Задача 18**: Реализуйте **сбор данных с 3 датчиков** (температура, влажность, освещённость) и отправку их в **PostgreSQL**.

### **Ресурсы**:
- [MicroPython для ESP32](https://docs.micropython.org/en/latest/esp32/tutorial/intro.html)
- [MQTT на MicroPython](https://randomnerdtutorials.com/micropython-mqtt-esp32-esp8266/)
- [GPIO в MicroPython](https://docs.micropython.org/en/latest/esp32/quickref.html)

---

## **📌 Этап 7: Проекты для портфолио (постоянно)**
### **Проекты**:
| **Проект**                     | **Технологии**                          | **Описание**                                  |
|--------------------------------|-----------------------------------------|-----------------------------------------------|
| **Система мониторинга IoT**     | FastAPI, PostgreSQL, MQTT, MAUI/Blazor  | Сбор данных с датчиков, визуализация в реальном времени. |
| **Чат-бот с NLP**              | Transformers, FastAPI, Blazor           | Бот для ответа на вопросы (например, поддержка пользователей). |
| **Распознавание лиц на ESP32**  | TensorFlow Lite, OpenCV, MicroPython    | Детекция лиц на микроконтроллере с отправкой уведомлений. |
| **Аналитика данных из PostgreSQL** | Pandas, FastAPI, Blazor            | Дашборд для визуализации данных (например, продажи, погода). |
| **Игра с ИИ**                  | Pygame, TensorFlow, C#                  | Простая игра (например, "Змейка") с ИИ-противником. |

### **Как презентовать**:
1. **GitHub**:
   - Код с **README** (описание, скриншоты, как запустить).
   - Пример структуры:
     ```
     /iot-monitoring
     ├── backend/          # FastAPI
     ├── frontend/         # Blazor/MAUI
     ├── scripts/          # Python-скрипты для ESP32
     ├── docker-compose.yml
     └── README.md
     ```
2. **Демо-видео**:
   - Запишите **5-минутный обзор** проекта (например, как работает IoT-мониторинг).
3. **Статьи**:
   - Опубликуйте пост на **Habr/Dev.to** с разбором ключевых моментов.

---
## **📅 Примерный график (4–5 месяцев)**
| **Этап**               | **Длительность** | **Результат**                                  |
|------------------------|------------------|-----------------------------------------------|
| Основы Python          | 2–3 недели       | Скрипты для анализа данных, классы для IoT.  |
| Продвинутый Python     | 2 недели         | Асинхронные скрипты, декораторы, многопоточность. |
| Веб-разработка         | 3 недели         | FastAPI/Django-приложения для бэкенда.        |
| AI/ML                  | 3–4 недели       | Модели для предсказаний, компьютерное зрение.|
| Интеграция с C#        | 2 недели         | gRPC/REST-сервисы, вызов Python из C#.       |
| IoT                    | 2 недели         | Скрипты для ESP32, обмен данными по MQTT.    |
| Проекты для портфолио  | Постоянно       | 2–3 завершённых проекта на GitHub.           |

---
## **💡 Советы от Senior Developer**
1. **Совмещайте Python с вашим основным стеком (C#)**:
   - Используйте **FastAPI** для бэкенда, который будет потреблять **Blazor/MAUI**.
   - Пример: FastAPI отдаёт данные для дашборда в Blazor.

2. **Для IoT**:
   - **MicroPython** проще, чем C/C++ для ESP32, если логика не требует максимальной производительности.
   - Отправляйте данные по **MQTT** (лёгкий протокол для IoT).

3. **Для AI/ML**:
   - Начните с **scikit-learn** (проще, чем TensorFlow).
   - Для встраиваемых систем используйте **TensorFlow Lite**.

4. **Интеграция с C#**:
   - **gRPC** быстрее, чем REST, для внутренних микросервисов.
   - **Python.NET** удобен для вызова Python-скриптов из C# (например, для ML-моделей).

5. **Оптимизация**:
   - Для тяжелых вычислений используйте **Cython** или **Numba**.
   - Избегайте глобальных переменных в асинхронном коде.